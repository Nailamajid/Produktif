const usia = 15;
if (usia >= 18) {
    console.log("Anda sudah dewasa.");
} else {
    console.log("Anda masih di bawah umur.");
}

const nilai= 80;
if (nilai >= 75) {
    console.log("Anda lulus.");
} else {
    console.log("Anda tidak lulus.");
}

const jam_tidur = 21_00;
if (jam_tidur >= 20_00) {
    console.log("selamat malam.");
} else {
    console.log("waktunya anda tidur.");
}

const suhu = 21;
if (suhu >= 35) {
    console.log("panas.");
} else {
    console.log("dingin.");
}

const berat_badan= 40;
if (berat_badan>= 51) {
    console.log("ideal.");
} else {
    console.log("kurus.");
}

let x = 100;
let y = 80;

// Contoh 1
let hasil1 = (x > 65) ? "Lebih besar dari 65" : "Tidak lebih besar dari 65";

// Contoh 2
let hasil2 = (x === y) ? "Sama" : "Tidak sama";

// Contoh 3
let hasil3 = (x % 2 === 0) ? "Genap" : "Ganjil";

// Contoh 4
let hasil4 = (x < y) ? x : y;

// Contoh 5
let hasil5 = (y > 150) ? "Positif" : (y < 0) ? "Negatif" : "Nol";

console.log("Hasil 1:", hasil1);
console.log("Hasil 2:", hasil2);
console.log("Hasil 3:", hasil3);
console.log("Hasil 4:", hasil4);
console.log("Hasil 5:", hasil5);

let x = 7;

// Contoh 1
let hasil1 = (x > 10) ? "Lebih besar dari 10" : (x < 10) ? "Kurang dari 10" : "Sama dengan 10";

// Contoh 2
let hasil2 = (x > 0) ? "Positif" : (x < 0) ? "Negatif" : "Nol";

// Contoh 3
let hasil3 = (x % 2 === 0) ? "Genap" : (x % 2 !== 0) ? "Ganjil" : "Bukan angka";

// Contoh 4
let hasil4 = (x > 5) ? "Lebih besar dari 5" : (x < 5) ? "Kurang dari 5" : "Sama dengan 5";

// Contoh 5
let hasil5 = (x === 10) ? "Sama dengan 10" : (x === 20) ? "Sama dengan 20" : "Bukan 10 atau 20";

console.log("Hasil 1:", hasil1);
console.log("Hasil 2:", hasil2);
console.log("Hasil 3:", hasil3);
console.log("Hasil 4:", hasil4);
console.log("Hasil 5:", hasil5);


